node_modules/
dist/
tmp/
# anchored to repo root so it ignores the build output dir, not
# .claude/skills/release/ (the release skill)
/release/
vendor/
.output/
/kura
*.db
*.db-wal
*.db-shm
.DS_Store
# Local-only working notes (see CLAUDE.md: public OSS policy)
TODO.md
.prompt
